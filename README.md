# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные

В ходе анализа проекта было установлено, что в приложении используются две сущности, которые описывают данные — товар и покупатель.

### Товар

```ts
interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}
```

**Поля интерфейса товара:**

- **id** — уникальный идентификатор товара
- **description** — описание товара
- **image** — ссылка на изображение товара
- **title** — название товара
- **category** — категория товара
- **price** — цена товара. Может быть `null`, если цена отсутствует

---

### Покупатель

```ts
interface IBuyer {
  payment: "card" | "cash" | "";
  email: string;
  phone: string;
  address: string;
}
```

**Поля интерфейса покупателя:**

- **payment** — выбранный способ оплаты
- **email** — адрес электронной почты покупателя
- **phone** — номер телефона покупателя
- **address** — адрес доставки

---

## Модели данных

В приложении используется несколько моделей данных. Они разделены между собой по смыслу и зонам ответственности.

Основные три модели данных:

- **Каталог товаров**
- **Корзина**
- **Покупатель**

---

## Каталог товаров (ProductCatalog)

### Назначение

Класс отвечает за хранение списка всех товаров, доступных в приложении.  
Также он хранит товар, который выбран пользователем для подробного отображения.

### Конструктор

```ts
constructor();
```

Создаёт экземпляр каталога товаров.

### Поля класса

**products: IProduct[]**

Массив всех товаров, доступных в приложении.

**selectedProduct: IProduct | null**

Товар, выбранный пользователем для подробного отображения.

### Методы

**set Products(products: IProduct[]): void**

Сохраняет массив товаров в каталоге.

Параметры:

- `products: IProduct[]` — массив товаров

---

**get Products(): IProduct[]**

Возвращает массив всех товаров.

---

**get ProductById(id: string): IProduct | undefined**

Возвращает товар по его идентификатору.

Параметры:

- `id: string` — идентификатор товара

---

**set SelectedProduct(product: IProduct): void**

Сохраняет товар для подробного отображения.

Параметры:

- `product: IProduct` — выбранный товар

---

**get SelectedProduct(): IProduct | null**

Возвращает товар, выбранный для подробного отображения.

---

## Корзина (ProductCart)

### Назначение

Класс отвечает за хранение товаров, добавленных пользователем в корзину.

### Конструктор

```ts
constructor();
```

Создаёт пустую корзину.

### Поля

**items: IProduct[]**

Массив товаров, добавленных пользователем в корзину.

### Методы

**getItems(): IProduct[]**

Возвращает массив товаров, находящихся в корзине.

---

**addItem(product: IProduct): void**

Добавляет товар в корзину.

Параметры:

- `product: IProduct` — товар для добавления

---

**removeItem(product: IProduct): void**

Удаляет товар из корзины.

Параметры:

- `product: IProduct` — товар для удаления

---

**clear(): void**

Очищает корзину.

---

**get TotalPrice(): number**

Возвращает общую стоимость товаров в корзине.

---

**get ItemsCount(): number**

Возвращает количество товаров в корзине.

---

**hasProduct(id: string): boolean**

Проверяет наличие товара в корзине.

Параметры:

- `id: string` — идентификатор товара

---

## Покупатель (CustomerData)

### Назначение

Класс хранит данные покупателя, которые пользователь вводит при оформлении заказа.

### Конструктор

```ts
constructor();
```

Создаёт объект покупателя.

### Поля

**payment: 'card' | 'cash' | ''**

Выбранный способ оплаты.

**email: string**

Адрес электронной почты покупателя.

**phone: string**

Номер телефона покупателя.

**address: string**

Адрес доставки заказа.

### Методы

**set Data(data: Partial<IBuyer>): void**

Сохраняет данные покупателя.  
Метод позволяет изменять только отдельные поля, не удаляя уже сохранённые значения.

---

**get Data(): IBuyer**

Возвращает все данные покупателя.

---

**clear(): void**

Очищает данные покупателя.

---

**validate(): Record<string, string>**

Проверяет корректность данных покупателя.

Поле считается **валидным**, если оно не пустое.

Метод возвращает объект с ошибками.

Пример результата:

```
{
  payment: "Не выбран вид оплаты",
  email: "Укажите email"
}
```

Если ошибок нет, метод возвращает пустой объект:

```
{}
```

## Слой представления (View)

В приложении используется несколько классов представления. Они разделены между собой по смыслу и зонам ответственности и отвечают за отображение данных на странице и реакцию на действия пользователя. Запросы к серверу из этого слоя не выполняются; связь с моделями и оформление заказа настраиваются в `main.ts` через события.

Базовый класс **`Component<T>`** описан в разделе «Базовый код». Остальные классы представления наследуются от него либо напрямую, либо через **`Form`** и **`Card`**.

Основные классы представления:

- **Базовая форма**
- **Базовая карточка товара**
- **Галерея**
- **Шапка**
- **Модальное окно**
- **Корзина (отображение)**
- **Форма оплаты и адреса**
- **Форма контактов**
- **Экран успешного заказа**
- **Карточка в каталоге**
- **Карточка предпросмотра**
- **Строка корзины**

Разметка интерфейса задаётся в `index.html` в элементах `<template id="...">`.

---

## Базовая форма (Form)

### Назначение

Класс отвечает за общую логику формы в интерфейсе. Он наследует `Component<IForm & T>`, где `IForm` описывает признак валидности и список ошибок, а `T` — поля конкретной формы. При вводе в поля и при отправке формы класс сообщает об этом через брокер событий `IEvents`. Правила имён событий приведены в разделе «События».

### Конструктор

```ts
constructor(container: HTMLFormElement, events: IEvents)
```

Связывает корневой элемент формы с брокером событий, подписывается на ввод в полях и на отправку формы. Атрибут `name` у элемента формы участвует в формировании имён событий.

### Поля класса

**container: HTMLFormElement**

Корневая форма на странице.

**formSubmit: HTMLButtonElement**

Кнопка отправки формы.

**formErrors: HTMLElement**

Область вывода текстов ошибок валидации.

**events: IEvents**

Брокер событий для оповещения о действиях пользователя.

### Методы

**render(state: Partial<IForm & T>): HTMLFormElement**

Обновляет состояние формы по переданным данным и возвращает элемент формы.

Параметры:

- `state` — частичные данные состояния, в том числе `valid`, `errors` и поля типа `T`

---

**set valid(value: boolean): void**

Включает или отключает кнопку отправки в зависимости от валидности формы.

Параметры:

- `value: boolean` — признак того, что форма может быть отправлена

---

**set errors(value: string[]): void**

Выводит переданные сообщения об ошибках одной строкой с разделителем `'; '`.

Параметры:

- `value: string[]` — список текстов ошибок

---

## Базовая карточка товара (Card)

### Назначение

Класс отвечает за отображение названия и цены товара в карточке. Это абстрактный базовый класс; он наследует `Component<T>`. Наследники добавляют остальную вёрстку и собственные обработчики нажатий.

### Конструктор

```ts
constructor(container: HTMLElement, events?: { onClick: (event: MouseEvent) => void })
```

Принимает корневой элемент карточки и опциональный объект `events` (для единообразия вызовов `super` у наследников). Базовый класс **не** вешает обработчики щелчков: он только находит узлы заголовка и цены. Подписку на клики реализуют наследники (`CardGallery`, `CardPreview`, `CardBasket`).

### Поля класса

**cardTitle: HTMLElement**

Узел с названием товара.

**cardPrice: HTMLElement**

Узел с ценой товара.

### Методы

**set title(value: string): void**

Задаёт отображаемое название товара.

Параметры:

- `value: string` — название

---

**set price(value: number | null): void**

Отображает цену в синапсах либо строку «Бесценно», если цена не задана.

Параметры:

- `value: number | null` — цена или отсутствие цены

---

## Галерея (Gallery)

### Назначение

Класс отвечает за область каталога на главной странице. Он наследует `Component<IGallery>` и хранит контейнер, в котором при обновлении данных показывается сетка карточек товаров.

### Конструктор

```ts
constructor(container: HTMLElement)
```

Создаёт галерею для переданного контейнера каталога.

### Поля класса

**galleryItem: HTMLElement**

Контейнер, в котором отображается список карточек (совпадает с переданным в конструкторе корнем).

### Методы

**set catalog(items: HTMLElement[]): void**

Заменяет содержимое каталога переданным набором элементов карточек.

Параметры:

- `items: HTMLElement[]` — готовые узлы карточек

---

## Шапка (Header)

### Назначение

Класс отвечает за шапку сайта: отображение числа товаров в корзине и кнопку перехода к корзине. Наследует `Component<IHeader>`. По нажатию на кнопку корзины инициирует событие её открытия.

### Конструктор

```ts
constructor(container: HTMLElement, events: IEvents)
```

Связывает разметку шапки с брокером событий и настраивает открытие корзины.

### Поля класса

**counterElement: HTMLElement**

Узел счётчика товаров в корзине.

**button: HTMLButtonElement**

Кнопка перехода к корзине.

**container: HTMLElement**

Корневой элемент шапки.

**events: IEvents**

Брокер событий.

### Методы

**set counter(value: number): void**

Обновляет отображаемое число в счётчике корзины.

Параметры:

- `value: number` — число для отображения

---

## Модальное окно (Modal)

### Назначение

Класс отвечает за модальное окно: подстановку содержимого, открытие и закрытие. Наследует `Component` с данными вида `{ content: HTMLElement }`. Окно закрывается по кнопке и по щелчку вне области содержимого; щелчок по содержимому окно не закрывает.

### Конструктор

```ts
constructor(container: HTMLElement)
```

Настраивает кнопку закрытия, закрытие по фону и остановку всплытия событий с блока содержимого.

### Поля класса

**closeButton: HTMLButtonElement**

Кнопка закрытия модального окна.

**contentModal: HTMLElement**

Область, куда подставляется содержимое окна.

### Методы

**set content(value: HTMLElement): void**

Подставляет переданный фрагмент в область содержимого модального окна.

Параметры:

- `value: HTMLElement` — корень вставляемого содержимого

---

**open(): void**

Открывает модальное окно.

---

**close(): void**

Закрывает модальное окно и очищает область содержимого.

---

**render(data: { content: HTMLElement }): HTMLElement**

Обновляет содержимое, открывает модальное окно и возвращает его корневой элемент.

Параметры:

- `data.content` — узел для показа внутри модального окна

---

## Корзина (отображение) (Basket)

### Назначение

Класс отвечает за отображение корзины: список позиций, итоговую сумму и кнопку оформления заказа. Наследует `Component` с полями списка и суммы. Пока корзина пуста, кнопка оформления недоступна; по её нажатию инициируется событие перехода к оформлению.

### Конструктор

```ts
constructor(container: HTMLElement, events: IEvents)
```

Связывает разметку корзины с брокером событий и отключает кнопку оформления до появления товаров.

### Поля класса

**list: HTMLElement**

Список позиций в корзине.

**basketTotal: HTMLElement**

Узел отображения итоговой суммы.

**basketButton: HTMLButtonElement**

Кнопка оформления заказа.

**events: IEvents**

Брокер событий.

### Методы

**set items(items: HTMLElement[]): void**

При непустом списке заполняет корзину и делает кнопку оформления активной; при пустом списке очищает корзину и снова отключает кнопку.

Параметры:

- `items: HTMLElement[]` — узлы строк корзины

---

**set total(value: number): void**

Отображает итоговую сумму заказа в синапсах.

Параметры:

- `value: number` — сумма для отображения

---

## Форма оплаты и адреса (Order)

### Назначение

Класс отвечает за форму выбора способа оплаты и ввода адреса доставки. Наследует `Form` с полями адреса и способа оплаты. При выборе способа оплаты сообщает об этом отдельным событием.

### Конструктор

```ts
constructor(container: HTMLFormElement, events: IEvents)
```

Помимо поведения базового `Form` подписывает кнопки выбора оплаты на отправку события смены способа оплаты.

### Поля

**buttons: HTMLButtonElement[]**

Кнопки выбора способа оплаты внутри формы.

Также у класса есть поля базового `Form`: `container`, `formSubmit`, `formErrors`, `events`.

### Методы

**set payment(name: string): void**

Подсвечивает выбранный способ оплаты в интерфейсе.

Параметры:

- `name: string` — имя выбранного способа оплаты

---

**set address(value: string): void**

Записывает адрес доставки в поле ввода с именем `address`, если оно есть в форме.

Параметры:

- `value: string` — адрес доставки

---

## Форма контактов (Contacts)

### Назначение

Класс отвечает за форму ввода электронной почты и телефона покупателя. Наследует `Form` с полями `email` и `phone`. Ввод и отправка обрабатываются так же, как в базовом классе `Form`.

### Конструктор

```ts
constructor(container: HTMLFormElement, events: IEvents)
```

Создаёт форму контактов с полным поведением класса `Form`.

### Методы

**set email(value: string): void**

Записывает адрес электронной почты в поле `email`.

Параметры:

- `value: string` — адрес почты

---

**set phone(value: string): void**

Записывает номер телефона в поле `phone`.

Параметры:

- `value: string` — номер телефона

---

## Экран успешного заказа (Succes)

### Назначение

Класс отвечает за сообщение об успешном оформлении заказа и кнопку закрытия экрана. Наследует `Component<{ total: number }>`.

### Конструктор

```ts
constructor(container: HTMLElement, events: { onClick: () => void })
```

Связывает кнопку закрытия с переданным обработчиком.

### Поля класса

**description: HTMLElement**

Текст с информацией о списанной сумме.

**close: HTMLButtonElement**

Кнопка закрытия экрана успеха.

### Методы

**set total(value: number): void**

Формирует текст о списанной сумме в синапсах.

Параметры:

- `value: number` — сумма заказа

---

## Карточка в каталоге (CardGallery)

### Назначение

Класс отвечает за карточку товара в сетке каталога. Наследует `Card` и добавляет изображение и категорию. Название и цена по-прежнему задаются унаследованными сеттерами базового `Card`. При передаче обработчика в конструктор выбор карточки выполняется щелчком по всей области карточки.

### Конструктор

```ts
constructor(container: HTMLElement, events?: { onClick: (event: MouseEvent) => void })
```

Вызывает конструктор `Card` и, если передан `events.onClick`, подписывает **контейнер карточки** на событие `click` (выбор товара по щелчку по всей карточке).

### Поля класса

**cardImage: HTMLImageElement**

Изображение товара в карточке.

**cardCategory: HTMLElement**

Категория товара в карточке.

### Методы

**set image(value: string): void**

Задаёт адрес изображения и подпись к нему.

Параметры:

- `value: string` — адрес файла изображения

---

**set category(value: string): void**

Отображает название категории и оформление по словарю категорий приложения.

Параметры:

- `value: string` — название категории

---

## Карточка предпросмотра (CardPreview)

### Назначение

Класс отвечает за подробную карточку товара в модальном окне: описание, изображение, категорию и кнопку действия. Наследует `Card`. Обработчик нажатия, если он задан, привязывается только к кнопке действия. Если цена не указана, кнопка недоступна и показывает текст «Недоступно».

### Конструктор

```ts
constructor(container: HTMLElement, events?: { onClick: (event: MouseEvent) => void })
```

Создаёт карточку предпросмотра; опциональный обработчик используется для кнопки действия, а не для всей карточки.

### Поля класса

**cardDescription: HTMLElement**

Текст описания товара.

**cardImage: HTMLImageElement**

Изображение товара.

**cardCategory: HTMLElement**

Категория товара.

**cardButton: HTMLButtonElement**

Кнопка действия (добавить в корзину / убрать и т.п.).

### Методы

**set description(value: string): void**

Задаёт текст описания товара.

Параметры:

- `value: string` — описание

---

**set image(value: string): void**

Задаёт изображение товара и альтернативный текст.

Параметры:

- `value: string` — адрес изображения

---

**set category(value: string): void**

Отображает категорию и её оформление.

Параметры:

- `value: string` — категория

---

**set price(value: number | null): void**

Обновляет цену по правилам базового `Card` и синхронно включает или отключает кнопку действия.

Параметры:

- `value: number | null` — цена или отсутствие цены

---

**set buttonTitle(value: string): void**

Задаёт подпись на кнопке действия, если кнопка активна.

Параметры:

- `value: string` — текст кнопки

---

## Строка корзины (CardBasket)

### Назначение

Класс отвечает за одну строку списка корзины: порядковый номер, название, цену и кнопку удаления позиции. Наследует `Card` с дополнительным полем индекса строки. Название и цена задаются унаследованными сеттерами `Card`. Если в конструктор передан обработчик щелчка, он назначается на кнопку удаления, а не на всю карточку.

### Конструктор

```ts
constructor(container: HTMLElement, events?: { onClick: (event: MouseEvent) => void })
```

Принимает корень строки и опциональный обработчик для кнопки удаления.

### Поля класса

**cardIndex: HTMLElement**

Узел с порядковым номером позиции.

**cardButton: HTMLButtonElement**

Кнопка удаления позиции из корзины.

### Методы

**set index(value: number): void**

Отображает порядковый номер строки в списке.

Параметры:

- `value: number` — номер позиции

---

## События

Модели и классы представления не вызывают друг друга напрямую: они инициируют события через `EventEmitter`. В `main.ts` на эти события подписываются обработчики, которые обновляют модели и снова вызывают отображение.

При добавлении в код нового вызова `emit` имеет смысл дополнить этот список: имя события, класс или место в коде, передаваемые данные (если они есть).

**Модели**

- `items:changed` — обновлён список товаров в каталоге (`ProductCatalog`). Дополнительных данных нет; по событию перестраивается галерея.
- `basket:changed` — изменилась корзина: добавление, удаление товара или очистка (`ProductCart`). Данных нет.
- `buyer:changed` — изменились данные покупателя (`CustomerData.setData`). Передаётся объект `IBuyer` для обновления форм и вывода ошибок валидации.

**Представление**

- `basket:open` — открытие корзины из шапки (`Header`).
- `order:open` — нажатие «Оформить» в корзине (`Basket`).
- `payment:change` — выбор способа оплаты (`Order`). Передаётся объект `{ target: string }`: в поле `target` — значение атрибута `name` нажатой кнопки (например `card` или `cash`).
- При вводе в поля формы класс `Form` инициирует событие вида `имяФормы.имяПоля:change`, например `order.address:change` или `contacts.email:change`. Передаётся `{ field, value }`.
- `order:submit` — отправка формы оплаты и адреса; далее показывается форма контактов.
- `contacts:submit` — отправка формы контактов; в `main.ts` выполняется запрос на сервер.

Правило формирования имён событий для полей описано в `View/Forms/Form.ts` (из атрибутов `name` у формы и у поля ввода).

**Инициация в `main.ts`**

События ниже инициируются из `main.ts` при создании карточек или строк корзины, а не внутри классов представления:

- `card:select` — выбор товара в каталоге; передаётся `IProduct`, открывается подробная карточка.
- `card:toggle-basket` — действие по кнопке на подробной карточке: добавить текущий выбранный товар в корзину или убрать его, затем закрыть модальное окно.
- `basket:remove-item` — нажатие кнопки удаления в строке корзины (колбэк задаётся при создании `CardBasket` в обработчике `basket:changed`); передаётся `{ id: string }`. Подписчик в `main.ts` удаляет товар из `ProductCart` по `id`.

---

# Слой коммуникации

Слой коммуникации отвечает за получение и отправку данных на сервер

## Класс ApiService

### Назначение

Отвечает за взаимодействие приложения с сервером: получение данных о товарах
и отправку данных заказа.
Класс использует **композицию** — содержит экземпляр класса `Api` и использует методы `get()` и `post()` для выполнения HTTP запросов.

### Конструктор

```ts
constructor(api: IApi);
```

Создаёт экземпляр сервиса с переданным экземпляром API.

Параметры:

- `api: IApi` — экземпляр класса Api для выполнения HTTP запросов

### Поля класса

**api: IApi**

Экземпляр класса Api, используемый для отправки запросов на сервер.

### Методы

**getProducts(): Promise<IProductsResponse>**

Получает список всех товаров с сервера вместе с информацией об их общем количестве.

Выполняет GET запрос на ендпоинт `/product` и возвращает объект со следующей структурой:

```ts
{
  total: number;
  items: IProduct[];
}
```

Пример использования:

```ts
const apiService = new ApiService(api);

apiService
  .getProducts()
  .then((response) => {
    console.log("Всего товаров:", response.total);
    console.log("Товары:", response.items);
  })
  .catch((error) => {
    console.error("Ошибка при получении товаров:", error);
  });
```

---

**postOrder(orderData: IOrderData): Promise<IOrderResponse>**

Отправляет на сервер данные заказа и получает подтверждение.

Параметры:

- `orderData: IOrderData` — объект с данными заказа:
  - `payment: 'card' | 'cash'` — способ оплаты
  - `email: string` — email покупателя
  - `phone: string` — телефон покупателя
  - `address: string` — адрес доставки
  - `items: string[]` — массив ID товаров в заказе
  - `total: number` — общая сумма заказа

Возвращает объект подтверждения `IOrderResponse`:

- `id: string` — ID оформленного заказа
- `total: number` — сумма заказа

Пример использования:

```ts
const orderData: IOrderData = {
  payment: "card",
  email: "customer@example.com",
  phone: "+7 (999) 123-45-67",
  address: "ул. Пушкина, д. 1",
  items: ["item-1", "item-2"],
  total: 5000,
};

apiService
  .postOrder(orderData)
  .then((response) => {
    console.log("Заказ оформлен, ID:", response.id);
    console.log("Сумма к оплате:", response.total);
  })
  .catch((error) => {
    console.error("Ошибка при оформлении заказа:", error);
  });
```

### Взаимодействие с моделью данных

После получения товаров с сервера их можно передать в модель данных `ProductCatalog`:

```ts
const apiService = new ApiService(api);
const catalog = new ProductCatalog();

apiService.getProducts().then((products) => {
  catalog.Products = products; // Сохраняем товары в каталог
});
```
